window.onload = function () {
    var config = {
      width: 800,
      height: 800,
      scene: [Scene1, Scene2],
      backgroundColor: 0x000000,
    };

    var game = new Phaser.Game(config);
  };